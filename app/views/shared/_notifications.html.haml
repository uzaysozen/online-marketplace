.toasts
  - flash.each do |name, msg|
    -#.alert.fade.in
    -#  .container
    -#    - if name == 'notice'
    -#      = icon('fas', 'check', class: 'flash-success')
    -#    - else
    -#      = icon('fas', 'warning', class: 'flash-error')
    -#    = msg
    -#    %a{ href: '#', title: 'Hide this message', data: { dismiss: :alert } } Dismiss
    -#
    .toast{"aria-atomic": "true", "aria-live": "assertive", "role": "alert", "data-delay": "5000"}
      .toast-header
        %strong.mr-auto
          - if name == 'notice'
            = icon('fas', 'info-circle')
          - else
            = icon('fas', 'exclamation-circle')
          = name.titleize
        %button.ml-2.mb-1.close{"aria-label": "Close", "data-dismiss": "toast", "type": "button"}
          %span{"aria-hidden" => "true"} ×
      .toast-body
        = msg